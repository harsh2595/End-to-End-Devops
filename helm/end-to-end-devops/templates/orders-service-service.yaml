apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: {{ .Values.namespace.name }}
spec:
  selector:
    app: orders-service
  ports:
    - port: {{ .Values.services.ordersService.port }}
      targetPort: {{ .Values.services.ordersService.port }}
